// =============================================================
// config.js — Configuration globale du dashboard
// =============================================================

const CONFIG = {
  locale: 'fr-FR',
};

export default CONFIG;
